{
  "name": "yepitshalal-catalogue",
  "private": true,
  "type": "module",
  "description": "Restaurant discovery and halal evidence pipeline. Kept out of the app's dependencies so Vercel never installs it.",
  "dependencies": {
    "@duckdb/node-api": "1.5.5-r.5",
    "sharp": "^0.34.3"
  },
  "scripts": {
    "test": "node tests/patterns.test.mjs && node tests/host-affinity.test.mjs"
  }
}
